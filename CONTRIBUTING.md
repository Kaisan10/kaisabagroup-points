# コントリビューションガイド

かい鯖グループポイントのオープンソース開発にご関心をお寄せいただきありがとうございます！
私たちは、誰もが参加しやすいコミュニティを目指しています。貢献していただくにあたり、いくつか重要なルールがありますので必ずお読みください。

## 開発の哲学と絶対のルール

このプロジェクトは「経済・ポイントシステム」という非常にデリケートなものを扱っています。
そのため、以下のルールは**絶対**に守っていただく必要があります。

### 1. ポイントは「無」から生まれない
- ユーザーのポイントが勝手に増減するようなコードは絶対にマージされません。
- 現在、ポイントが「無から有へ」生まれるのは、フォーラム活動等による正規のWebhook経由のみです。
- ポイントが移動する場合は必ず「誰かから減って、誰かに増える」というゼロサム原則を遵守してください。

### 2. データベース操作の安全性 (超重要)
ポイントの移動や、残高に関する処理を追加・変更する場合は、以下の手順を守ってください。

- **トランザクションの使用**: 処理は必ず `BEGIN` で開始し、成功時に `COMMIT`、エラー時に `ROLLBACK` を行ってください。
- **排他ロック（行ロック）**: ポイントを更新する前に必ず `FOR UPDATE` を使用して行をロックし、同時実行時の二重支払いやデータ競合（レースコンディション）を防いでください。
- **CHECK制約**: 残高がマイナスにならないことをDBレイヤーでも担保するように意識してください。

### 3. セキュリティを最優先
- 新しいAPIエンドポイントを追加する場合は、適切なレートリミット（`express-rate-limit`）を設定してください。
- クライアントからの入力（金額など）は決して信用せず、サーバー側で必ずバリデーションを行ってください。
- CSRFやXSSに関する脆弱性を生み出さないよう、慎重に実装してください。

## 開発フロー

1. このリポジトリをフォークし、手元にクローンします。
2. 修正・機能追加用の新しいブランチを作成します（例: `feature/add-new-api` または `fix/csrf-bug`）。
3. 変更を加え、ローカルで十分にテストを実施します（`npm run test` 等で既存のテストが壊れていないか確認してください）。
4. 変更をコミットし、フォークしたリポジトリにプッシュします。
5. このリポジトリに向けてPull Requestを作成します。

### Pull Requestを作成する際の注意点
- なぜその変更が必要なのか（背景・理由）をPRの概要に必ず明記してください。
- 複雑なロジックを追加した場合は、それがどのような挙動をするのかコメントを残してください。
- 変更部分に対する自動テストを追加してください。

皆さまからの素晴らしいアイデアやコードをお待ちしております！
